const NotFound = () => {
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h2>404, the page you are looking for does not exist</h2>
    </div>
  )
}

export default NotFound
